# Typescript Into and Notes

- Intro
    - Why Learn TypeScript? 
        - App- Crashing Runtime errors are dramatically reduced by Typescript, which checks the code at compile time before it ever gets deployed
        - Using typescript "turns on" a number of neat features that make your life as a dev easier. 
        - Improve Employability! Since it is used in multiple places! 

    - Pizza App Example! 
        - See Pizza App Example 

    - TS is smart enough to figure out what our types are! 
        - If the types get reassigned and the types do not match, TS will throw an error. 
        
